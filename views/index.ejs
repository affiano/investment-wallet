<meta name="viewport" content="width=device-width, initial-scale=1.0">
<script>
    function goToWallet(e) {
        e.preventDefault();

        var url = '/check-balance';

        var xmlHttp = new XMLHttpRequest();
        xmlHttp.onreadystatechange = function () {
            // console.log('xmlHttp.responseText', xmlHttp.responseText);
            if (xmlHttp.readyState === 4) {
                // var res = JSON.parse(xmlHttp.responseText);
            }
        };

        xmlHttp.open('POST', url, true);
        xmlHttp.setRequestHeader('Content-Type', 'application/json');
        xmlHttp.send();
    }
</script>

<h2>Hi <%= name %>
</h2>
<div>
    <form action="/check-balance" method="POST">
        <button type="submit">My Wallet</button>
    </form>
</div>
<a href="/buy-fund"><button>Buy Funds</button></a>
<br /><br />
<a href="/orders"><button>My Funds</button></a>
<br /><br />
<a href="/transactions"><button>My Transactions</button></a>
<br /><br />
<form action="/logout?_method=DELETE" method="POST">
    <button type="submit">Log Out</button>
</form>